import React from "react";
import { Text } from "native-base";

const ShowCardDetail = ({ navigate }) => {
  return (
    <div>
      <Text>Showing show details</Text>
    </div>
  );
};

export default ShowCardDetail;
